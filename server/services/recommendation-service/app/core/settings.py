from core.config import settings

# This file can be used to re-export settings or configure environment specific overrides.
# For now, we just export the centralized settings.
def get_settings():
    return settings
